---
description: Implement one WebPi feature with focused TDD, compatibility review, and fresh validation.
---
Implement this WebPi feature: $@

Before editing:
- inspect runtime, exact Project/Session, Git/workspace state, relevant source/tests, and tool/extension schemas;
- state the observable user outcome and infer only non-load-bearing details;
- preserve unrelated work and current compatibility/authority boundaries.

Implementation loop:
1. Add/strengthen a focused failing test when practical.
2. Make the smallest coherent vertical change.
3. Run the targeted test.
4. Run the relevant broader package/module gate.
5. Inspect the final diff for security, recovery, compatibility, WebPi naming, and user guidance.

Prefer structured WebPi tools over shell. Reconcile outcome_unknown before retrying. Do not weaken tests or guards.
Do not commit, deploy, push, install executable packages, elevate trust, rotate credentials, or destructively clean up unless explicitly authorized.

Finish with: Changed / Validated / Remaining risk / User action.
