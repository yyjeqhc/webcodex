# Example config for webcodex-agent.
# Copy to /etc/webcodex/agent.toml and edit token/client_id/server_url.
# Prefer generating this file with: webcodex-cli agent init
# The compatibility command webcodex-agent init still works.

server_url = "https://webcodex.example.com"
token = "replace-with-agent-token"
client_id = "example-agent"
display_name = "Example Agent"
owner = "example-user"
transport = "websocket"
poll_interval_ms = 1000
projects_dir = "/etc/webcodex/projects.d"

# Shell execution defaults to program = "sh" and args = ["-c"]. systemd does
# not read ~/.bashrc, so expose tools such as Rust/Cargo explicitly here or via
# systemd Environment=PATH. init_script is opt-in only and is sourced before the
# requested command when configured. runtime_status/listAgents do not expose
# init_script.
#[shell]
#program = "/bin/bash"
#args = ["-lc"]
#path_prepend = ["/home/webcodex/.cargo/bin"]
#env = { CARGO_HOME = "/home/webcodex/.cargo", RUSTUP_HOME = "/home/webcodex/.rustup" }
#init_script = "/home/webcodex/.config/webcodex/shell-env.sh"

[capabilities]
file_read = true
file_write = true
git = true
shell = true
jobs = true
async_jobs = true
async_shell_jobs = true

[policy]
# Missing or empty allowed_roots defaults to $HOME.
# Explicit allowed_roots replaces that default. Leave it empty to use $HOME, or
# set a narrower workspace root for production hardening.
allow_raw_shell = true
allow_cwd_anywhere = false
# allowed_roots = []
# Example narrow root only; not the default:
# allowed_roots = ["/root/git"]
max_timeout_secs = 3600
max_output_bytes = 262144
