use super::RouteAuth::AuthMiddleware;
use super::{
    route, AuditClass::*, RouteAuth, RouteId::*, RouteMethod::*, RouteOpenApiProjection::*,
    RouteSpec, RouteSurface::*,
};
use webcodex_core::authority::{
    OAuthRouteScopePolicy::*, SCOPE_COMMUNICATION_MANAGE, SCOPE_COMMUNICATION_READ,
    SCOPE_PROJECT_READ, SCOPE_RUNTIME_READ, SCOPE_SESSION_COLLABORATE,
};

pub(super) const PUBLIC_WEB_ROUTES: &[RouteSpec] = &[
    route(
        RuntimeWebRoot,
        Get,
        "/runtime",
        Public,
        PublicWeb,
        Hidden,
        Other,
        RouteAuth::Public,
    ),
    route(
        RuntimeWebAppJs,
        Get,
        "/runtime/app.js",
        Public,
        PublicWeb,
        Hidden,
        Other,
        RouteAuth::Public,
    ),
    route(
        RuntimeWebStylesCss,
        Get,
        "/runtime/styles.css",
        Public,
        PublicWeb,
        Hidden,
        Other,
        RouteAuth::Public,
    ),
    route(
        AdminWebRoot,
        Get,
        "/admin",
        Public,
        PublicWeb,
        Hidden,
        Other,
        RouteAuth::Public,
    ),
    route(
        AdminWebAppJs,
        Get,
        "/admin/app.js",
        Public,
        PublicWeb,
        Hidden,
        Other,
        RouteAuth::Public,
    ),
    route(
        AdminWebStylesCss,
        Get,
        "/admin/styles.css",
        Public,
        PublicWeb,
        Hidden,
        Other,
        RouteAuth::Public,
    ),
];

// Runtime Console routes use ordinary delegated runtime/project/session scopes.
pub(super) const ROUTES: &[RouteSpec] = &[
    route(
        RuntimeConsoleExtensions,
        Post,
        "/api/runtime-console/extensions",
        Require(SCOPE_PROJECT_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleInstruction,
        Post,
        "/api/runtime-console/instruction",
        Require(SCOPE_PROJECT_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleProjectGit,
        Post,
        "/api/runtime-console/project-git",
        Require(SCOPE_PROJECT_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsolePluginReload,
        Post,
        "/api/runtime-console/plugin-reload",
        Require(SCOPE_PROJECT_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleOverview,
        Post,
        "/api/runtime-console/overview",
        Require(SCOPE_RUNTIME_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleRunner,
        Post,
        "/api/runtime-console/runner",
        Require(SCOPE_RUNTIME_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleWindows,
        Post,
        "/api/runtime-console/windows",
        Require(SCOPE_RUNTIME_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleWindow,
        Post,
        "/api/runtime-console/window",
        Require(SCOPE_RUNTIME_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleWindowCollaboration,
        Post,
        "/api/runtime-console/window-collaboration",
        Require(SCOPE_SESSION_COLLABORATE),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleWindowCollaborationPost,
        Post,
        "/api/runtime-console/window-collaboration-post",
        Require(SCOPE_SESSION_COLLABORATE),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleProjects,
        Post,
        "/api/runtime-console/projects",
        Require(SCOPE_PROJECT_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleGoals,
        Post,
        "/api/runtime-console/goals",
        Require(SCOPE_COMMUNICATION_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleGoal,
        Post,
        "/api/runtime-console/goal",
        Require(SCOPE_COMMUNICATION_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleWorkflowSessions,
        Post,
        "/api/runtime-console/workflow-sessions",
        Require(SCOPE_PROJECT_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleWorkflowSessionLocate,
        Post,
        "/api/runtime-console/workflow-session-locate",
        Require(SCOPE_PROJECT_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleWorkflowSession,
        Post,
        "/api/runtime-console/workflow-session",
        Require(SCOPE_PROJECT_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleWorkflowSessionMessages,
        Post,
        "/api/runtime-console/workflow-session-messages",
        Require(SCOPE_RUNTIME_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleWorkflowSessionObserve,
        Post,
        "/api/runtime-console/workflow-session-observe",
        Require(SCOPE_RUNTIME_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleWorkflowSessionPostMessage,
        Post,
        "/api/runtime-console/workflow-session-post-message",
        Require(SCOPE_SESSION_COLLABORATE),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleWorkflowSessionWithdrawMessage,
        Post,
        "/api/runtime-console/workflow-session-withdraw-message",
        Require(SCOPE_SESSION_COLLABORATE),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleWorkflowSessionReplaceMessage,
        Post,
        "/api/runtime-console/workflow-session-replace-message",
        Require(SCOPE_SESSION_COLLABORATE),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleCommunicationAgents,
        Post,
        "/api/runtime-console/communication/agents",
        Require(SCOPE_COMMUNICATION_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleCommunicationAgentCreate,
        Post,
        "/api/runtime-console/communication/agent/create",
        Require(SCOPE_COMMUNICATION_MANAGE),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleCommunicationAgentUpdate,
        Post,
        "/api/runtime-console/communication/agent/update",
        Require(SCOPE_COMMUNICATION_MANAGE),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleCommunicationEndpointAttach,
        Post,
        "/api/runtime-console/communication/endpoint/attach",
        Require(SCOPE_COMMUNICATION_MANAGE),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleCommunicationEndpointRenew,
        Post,
        "/api/runtime-console/communication/endpoint/renew",
        Require(SCOPE_COMMUNICATION_MANAGE),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleCommunicationEndpointDetach,
        Post,
        "/api/runtime-console/communication/endpoint/detach",
        Require(SCOPE_COMMUNICATION_MANAGE),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleCommunicationConversations,
        Post,
        "/api/runtime-console/communication/conversations",
        Require(SCOPE_COMMUNICATION_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleCommunicationConversationCreate,
        Post,
        "/api/runtime-console/communication/conversation/create",
        Require(SCOPE_COMMUNICATION_MANAGE),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleCommunicationConversation,
        Post,
        "/api/runtime-console/communication/conversation",
        Require(SCOPE_COMMUNICATION_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleCommunicationMessagePost,
        Post,
        "/api/runtime-console/communication/message/post",
        Require(SCOPE_COMMUNICATION_MANAGE),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleCommunicationInbox,
        Post,
        "/api/runtime-console/communication/inbox",
        Require(SCOPE_COMMUNICATION_READ),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
    route(
        RuntimeConsoleCommunicationInboxConsume,
        Post,
        "/api/runtime-console/communication/inbox/consume",
        Require(SCOPE_COMMUNICATION_MANAGE),
        RuntimeConsole,
        Hidden,
        Other,
        AuthMiddleware,
    ),
];
